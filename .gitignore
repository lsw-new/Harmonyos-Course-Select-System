/node_modules
/oh_modules
/local.properties
/.idea
/.claude/settings.local.json
/.mcp.json
**/build
/.hvigor
**/.hvigor
.cxx
/.clangd
/.clang-format
/.clang-tidy
**/.test
/.appanalyzer
/.codegraph
/harmonyos-course-select-system

# Node 后端测试覆盖率产物
**/coverage

# HarmonyOS release signing materials - never commit private keys/certs/profiles/passwords
**/*.p12
**/*.p7b
**/*.cer
**/*.csr
**/*.key
**/*.pem
**/*.keystore
**/*.jks
**/*profile*
**/*provision*
**/*password*
**/*secret*

# 例外：上面的宽泛保密名规则会误伤同名 ArkTS 源码页（如 AdminPasswordPage.ets、
# AdminProfilePage.ets）。这里放行 entry 源码树下的 .ets 页面；真正的密钥/证书/
# 签名材料仍按扩展名(.p12/.key/.pem/.keystore 等)被忽略。
!entry/src/**/*Password*.ets
!entry/src/**/*Profile*.ets
!entry/src/**/*Secret*.ets
!entry/src/**/*Provision*.ets

# 例外：server 后端源码（CommonJS）同样会被上面的保密名规则误伤（如 profile.repo.js）。
# 放行整个 server/src 与 server/test 源码树；真正的密钥/凭据在 server/.env（不在源码树），另行处置。
!server/src/**
!server/test/**
# 数据库迁移亦须始终入库（如 020_teacher_profiles.sql 含 "profile" 会被上面规则误伤）。
!server/migrations/**
